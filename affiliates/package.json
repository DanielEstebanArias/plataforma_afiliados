{
  "name": "plataforma-afiliados",
  "version": "1.0.0",
  "private": true,
  "description": "Árbol de afiliados con formularios configurables y acceso por rama",
  "scripts": {
    "start": "node ../scripts/start-affiliates.cjs",
    "test": "node --test tests/*.test.cjs",
    "native:sync": "cap sync",
    "native:android": "cap open android",
    "native:ios": "cap open ios"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
